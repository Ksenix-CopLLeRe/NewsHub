# Docker-контейнеризация микросервиса реакций

## 🐳 Информация о работе

**Студент:** [твоё имя]
**Микросервис:** Reactions & Counters Service
**Дата:** 26.02.2026

## 📋 Чек-лист выполнения

### Dockerfile
- [x] Используется базовый образ python:3.11-slim
- [x] Есть инструкция WORKDIR
- [x] Копируется requirements.txt
- [x] Зависимости устанавливаются через RUN pip install
- [x] Код НЕ копируется (используется bind mount)
- [x] Корректная CMD для запуска

### Сборка и запуск
- [x] `docker build -t reactions-service .` выполняется без ошибок
- [x] `docker run -p 8000:8000 -v $(pwd):/app reactions-service` запускает приложение
- [x] Приложение доступно по http://localhost:8000
- [x] Swagger UI доступен по http://localhost:8000/docs

### Live-reload
- [x] Изменение текста в main.py приводит к перезапуску
- [x] Изменения видны в браузере после перезагрузки

### Команды управления
- [x] `docker ps` показывает запущенный контейнер
- [x] `docker stop <ID>` останавливает контейнер
- [x] `docker start <ID>` запускает снова
- [x] `docker logs <ID>` показывает логи
- [x] `docker exec -it <ID> bash` позволяет зайти внутрь

## 🚀 Инструкция по запуску

```bash
# 1. Собрать образ
docker build -t reactions-service .

# 2. Запустить контейнер
docker run -p 8000:8000 -v $(pwd):/app reactions-service

# 3. Открыть в браузере
http://localhost:8000/docs
```

## 📸 Скриншоты

*(сделай скриншоты и добавь сюда)*

1. Запущенный контейнер (docker ps)
2. Swagger UI с эндпоинтами
3. Пример ответа /reactions/counts/{newsId}

## ✅ Вывод

В ходе работы я научилась:
- Писать Dockerfile для Python-приложения
- Собирать Docker-образ
- Запускать контейнер с пробросом портов
- Использовать bind mount для разработки
- Управлять контейнерами (start, stop, logs, exec)