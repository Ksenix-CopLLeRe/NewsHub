# Стандарты API проекта NewsHub

## Общие договорённости между микросервисами

### 1. Форматы данных

#### Даты и время
- Используем формат **ISO 8601** в UTC
- Пример: `2025-03-01T18:00:00Z`
- Во всех ответах API даты возвращаются в этом формате
- При передаче дат в запросах (если нужно) используем этот же формат

#### Идентификаторы
- **ID пользователей**: целые числа (integer)
- **ID новостей**: строки (string), содержащие URL новости
- **ID реакций**: целые числа (integer)

#### Типы реакций
Фиксированный набор строковых значений:
- `important` - 🔥 важно
- `interesting` - 🤔 интересно
- `shocking` - 😱 шокирует
- `useful` - 💡 полезно
- `liked` - ❤️ нравится

### 2. Пагинация

Для эндпоинтов, возвращающих списки, используем:
- Параметры запроса: `page` (номер страницы, начиная с 1) и `size` (элементов на странице)
- Ответ содержит:
  - `items`: массив элементов
  - `total`: общее количество элементов
  - `page`: текущая страница
  - `size`: размер страницы

### 3. Формат ошибок

Все ошибки возвращаются в едином формате:

```json
{
  "error": "Описание ошибки для человека",
  "code": 400,
  "details": {
    "поле": ["список ошибок валидации"]
  }
}